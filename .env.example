# Flight Pulse — environment variables (values only, never commit real secrets)

# LLM provider for the AI Operations Brief + Ask Flight Pulse (DeepSeek,
# OpenAI-compatible chat-completions API).
DEEPSEEK_API_KEY=

# Optional: override the DeepSeek base URL / model.
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat

# Live flight-status data (AviationStack). Free tier is ~100 requests/month.
AVIATIONSTACK_API_KEY=

# Optional live-data tuning (defaults are conservative for the free tier):
# AVIATIONSTACK_CARRIERS=UA,AA,DL,WN,B6,AS,NK,F9   # carriers sampled per refresh
# AVIATIONSTACK_TTL_MS=600000                      # cache TTL (10 min)
# AVIATIONSTACK_MAX_REQUESTS=80                    # monthly request budget
